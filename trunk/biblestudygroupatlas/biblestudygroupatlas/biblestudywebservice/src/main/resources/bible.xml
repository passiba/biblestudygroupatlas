<?xml version="1.0" encoding="UTF-8"?>

<config charset="UTF-8">

      
    <file action="write" path="${outputSubDir}${filename}" charset="UTF-8">
        <template>
            <![CDATA[
            <?xml version="1.0" encoding="UTF-8"?>
             <chapters date="${sys.datetime("dd.MM.yyyy")}"> ]]>
        </template>
        
        <loop item="articleUrl" index="i">
            <!-- collects URLs of all articles from the front page -->
            <list>
                <xpath expression="//body/dt/a/@href">
                    <html-to-xml>
                        <http url="${startUrl}"/>
                    </html-to-xml>
                </xpath>
                <!--  <xpath expression="//div[@class='story' or @class='story headline']/a[1]/@href">
                    <html-to-xml>
                        <http url="${startUrl}"/>
                    </html-to-xml>
                </xpath>-->
            </list>
            
            <!-- downloads each chapter and extract data from it -->
            <body>
                <xquery>
                    <xq-param name="doc">
                        <html-to-xml>
                            <http url="${sys.fullUrl(startUrl, articleUrl)}?&amp;pagewanted=print"/>
                        </html-to-xml>
                    </xq-param>
                    <xq-expression><![CDATA[
                        declare variable $doc as node() external;
                        
                       
                        let $title := data($doc//h2)
                        let $subtitle := data($doc//h4)
                        let $number := data($doc//p/i/a)
                        let $text := data($doc//p)
                            return
                                <chapter>
                                    <title>{data($title)}</title>
                                    <subtitle>{data($subtitle)}</subtitle>
                                    <number>{data($number)}</number>
                                    <text>{data($text)}</text>
                                </chapter>
                    ]]></xq-expression>
                </xquery>
            </body>
        </loop>
        
        
        
        <![CDATA[ </chapters> ]]>
    </file>
    
</config>